function encode(){
  var b = document.querySelector(".box").value;
  var c = document.querySelector("#output");
  var a = Math.pow(Math.floor(Math.random() * 694206660 - 10000000) + 10000000, 2);
  b = b.replace(/a/g, 28 * a);
  b = b.replace(/b/g, 27 * a);
  b = b.replace(/c/g, 3 * a);
  b = b.replace(/d/g, 4 * a);
  b = b.replace(/e/g, 5 * a);
  b = b.replace(/f/g, 6 * a);
  b = b.replace(/g/g, 7 * a);
  b = b.replace(/h/g, 8 * a);
  b = b.replace(/i/g, 9 * a);
  b = b.replace(/j/g, 10 * a);
  b = b.replace(/k/g, 11 * a);
  b = b.replace(/l/g, 12 * a);
  b = b.replace(/m/g, 13 * a);
  b = b.replace(/n/g, 14 * a);
  b = b.replace(/o/g, 15 * a);
  b = b.replace(/p/g, 16 * a);
  b = b.replace(/q/g, 17 * a);
  b = b.replace(/r/g, 18 * a);
  b = b.replace(/s/g, 19 * a);
  b = b.replace(/t/g, 20 * a);
  b = b.replace(/u/g, 21 * a);
  b = b.replace(/v/g, 22 * a);
  b = b.replace(/w/g, 23 * a);
  b = b.replace(/x/g, 24 * a);
  b = b.replace(/y/g, 25 * a);
  b = b.replace(/z/g, 26 * a);
  b = b.replace(/0/g, 'A');
  b = b.replace(/1/g, 'H');
  b = b.replace(/2/g, 'P');
  b = b.replace(/3/g, 'J');
  b = b.replace(/4/g, 'I');
  b = b.replace(/5/g, 'Y');
  b = b.replace(/6/g, 'X');
  b = b.replace(/7/g, 'Z');
  b = b.replace(/8/g, 'Q');
  b = b.replace(/9/g, 'D');
  c.innerHTML = "Key:" + a;
  c.innerHTML = c.innerHTML + "\n" + "Encryption value:" + b;
}
function decode(){
  var a = document.querySelector(".key").value;
  var c = document.querySelector("#output");
  var x = document.querySelector(".box").value;
  x = x.replace(/A/g, '0');
  x = x.replace(/H/g, '1');
  x = x.replace(/P/g, '2');
  x = x.replace(/J/g, '3');
  x = x.replace(/I/g, '4');
  x = x.replace(/Y/g, '5');
  x = x.replace(/X/g, '6');
  x = x.replace(/Q/g, '8');
  x = x.replace(/D/g, '9');
  x = x.replace(/Z/g, '7');
  x = x.replace(new RegExp(`${a * 28}`, "g"), 'a');
  x = x.replace(new RegExp(`${a * 27}`, "g"), 'b');
  x = x.replace(new RegExp(`${a * 3}`, "g"), 'c');
  x = x.replace(new RegExp(`${a * 4}`, "g"), 'd');
  x = x.replace(new RegExp(`${a * 5}`, "g"), 'e');
  x = x.replace(new RegExp(`${a * 6}`, "g"), 'f');
  x = x.replace(new RegExp(`${a * 7}`, "g"), 'g');
  x = x.replace(new RegExp(`${a * 8}`, "g"), 'h');
  x = x.replace(new RegExp(`${a * 9}`, "g"), 'i');
  x = x.replace(new RegExp(`${a * 10}`, "g"), 'j');
  x = x.replace(new RegExp(`${a * 11}`, "g"), 'k');
  x = x.replace(new RegExp(`${a * 12}`, "g"), 'l');
  x = x.replace(new RegExp(`${a * 13}`, "g"), 'm');
  x = x.replace(new RegExp(`${a * 14}`, "g"), 'n');
  x = x.replace(new RegExp(`${a * 15}`, "g"), 'o');
  x = x.replace(new RegExp(`${a * 16}`, "g"), 'p');
  x = x.replace(new RegExp(`${a * 17}`, "g"), 'q');
  x = x.replace(new RegExp(`${a * 18}`, "g"), 'r');
  x = x.replace(new RegExp(`${a * 19}`, "g"), 's');
  x = x.replace(new RegExp(`${a * 20}`, "g"), 't');
  x = x.replace(new RegExp(`${a * 21}`, "g"), 'u');
  x = x.replace(new RegExp(`${a * 22}`, "g"), 'v');
  x = x.replace(new RegExp(`${a * 23}`, "g"), 'w');
  x = x.replace(new RegExp(`${a * 24}`, "g"), 'x');
  x = x.replace(new RegExp(`${a * 25}`, "g"), 'y');
  x = x.replace(new RegExp(`${a * 26}`, "g"), 'z');
  c.innerHTML = x;
}